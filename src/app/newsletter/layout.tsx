export const metadata = {
  title: "The Solo Founder Weekly | OneFoundr",
  description: "Subscribe to weekly actionable strategies for solo founders—frameworks, tools, and quick wins delivered every Tuesday.",
};

export default function NewsletterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
